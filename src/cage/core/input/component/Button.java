package cage.core.input.component;

public enum Button implements IInputComponent {
    NONE,
    // _1, _2, _3,
    _4, _5, _6, _7, _8,
    LEFT, MIDDLE, RIGHT,
    // CROSS, CIRCLE, SQUARE, TRIANGLE,
    A, B, X, Y,
    BACK, START, GUIDE,
    DPAD_DOWN, DPAD_LEFT, DPAD_RIGHT, DPAD_UP,
    LEFT_BUMPER, LEFT_THUMB, RIGHT_BUMPER, RIGHT_THUMB
}
